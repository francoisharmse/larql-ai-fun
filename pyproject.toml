[project]
name = "larql-audio-app"
version = "0.1.0"
description = "LARQL audio fun app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aws-cdk-lib>=2.192.0",
    "fastapi>=0.115.12",
    "flask>=3.1.0",
]
